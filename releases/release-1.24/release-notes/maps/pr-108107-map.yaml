pr: 108107
releasenote:
  text: kubelet no longer forcefully closes active connections on heartbeat failures, using
    the http2 health check mechanism to detect broken connections. Users can force
    the previous behavior of the kubelet by setting the environment variable DISABLE_HTTP2.
