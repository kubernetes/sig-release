pr: 132886
releasenote:
  text: |-
    API calls dispatched during pod scheduling were executed asynchronously if the `SchedulerAsyncAPICalls` feature gate was enabled.
    Out-of-tree plugins used `APIDispatcher` and `APICacher` from the framework to dispatch their own calls.
pr_body: ""
