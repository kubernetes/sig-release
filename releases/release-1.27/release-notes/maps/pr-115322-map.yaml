pr: 115322
releasenote:
  text: Fixed a bug that caused to panic the apiserver when trying to allocate a Service
    with a dynamic `ClusterIP` and it has been configured with Service CIDRs with
    a /28 mask for IPv4 and a /124 mask for IPv6
