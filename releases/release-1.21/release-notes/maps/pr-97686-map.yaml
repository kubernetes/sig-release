pr: 97686
releasenote:
  text: Kubectl `--ignore-errors` is now honored by all containers, maintaining consistency
    with parallelConsumeRequest behavior -- fixes a bug where `kubectl logs` may terminate
    early on first error.
